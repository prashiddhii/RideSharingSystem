@model RideSharingSystem.Models.Ride

<h2>Create New Ride</h2>

<form method="post" action="/Home/Create">
    @Html.AntiForgeryToken()

    <div class="form-group mb-3">
        <label>Start Location</label>
        <input type="text" name="StartLocation" class="form-control" required />
    </div>

    <div class="form-group mb-3">
        <label>End Location</label>
        <input type="text" name="EndLocation" class="form-control" required />
    </div>

    <div class="form-group mb-3">
        <label>Driver ID</label>
        <input type="number" name="DriverId" class="form-control" value="2" required />
        <small class="text-muted">Use 2 for Bob, 3 for Ravu</small>
    </div>

    <div class="form-group mb-3">
        <label>Available Seats</label>
        <input type="number" name="AvailableSeats" class="form-control" min="1" max="10" required />
    </div>

    <div class="form-group mb-3">
        <label>Price (Rs)</label>
        <div class="input-group">
            <span class="input-group-text">Rs</span>
            <input type="number" name="Price" class="form-control" step="0.01" min="0" max="10000" required />
        </div>
    </div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary">Create Ride</button>
        <a href="/Home/Index" class="btn btn-secondary">Back to List</a>
    </div>
</form>

@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger mt-3">
        <ul>
            @foreach (var modelState in ViewData.ModelState.Values)
            {
                foreach (var error in modelState.Errors)
                {
                    <li>@error.ErrorMessage</li>
                }
            }
        </ul>
    </div>
}